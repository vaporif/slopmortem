# Corporate hierarchy overrides. CODEOWNERS-protected.
#
# Single-domain corporate hierarchies (JPMorgan/Chase, Alphabet/Google, etc.)
# that the suffix-delta heuristic alone misses. Each override names a parent
# canonical and a list of subsidiary canonicals; tier-1 entity resolution
# treats the listed pair as a parent_subsidiary case and demotes to tier-2.
version: "1"
overrides: []
